exit
files.find_all {|i| i.downcase.include? "#{name.downcase}"}.find_all{|i| i.downcase.include? "#{lastname.downcase}"}
files.find_all {|i| i.downcase include? "#{name.downcase}"}.find_all{|i| i.downcase.include? "#{lastname.downcase}"}
exit
name.downcase
name.up
name.to_up
files.find_all {|i| i.include? "yusmila"}.find_all{|i| i.include? "a"}
files.find_all {|i| i.include? "Yusmila"}.find_all{|i| i.include? "a"}
files.find_all {|i| i.include? "Yusmila"}.find_all{|i| i.include? "#{last_name}"}
files.find_all {|i| i.include? "#{name}"}.find_all{|i| i.include? "#{last_name}"}
files.find_all {|i| i.include? "#{name}"}
files.find_all {|i| i.include? "#{name}" && i.include? "#{lastname}"}
files.find_all {|i| i.include? "#{name}" && i.incliude? "#{lastname}"}
files.find_all {|i| i.include? "#{name}" && "#{lastname}"}
name = "Felipe"
files.find_all {|i| i.include? "#{name}" && "#{lastname}"}
files
lastname
name
files.find_all {|i| i.include? "#{name}" && "#{lastname}"}
c
files.find_all {|i| i.include? "#{name}" && "#{lastname}"}
files.find_all {|i| i.include? "#{name}" && "#{lastname}"
results
lastname
name
c
